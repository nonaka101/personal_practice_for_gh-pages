# `03_roll-dice-2`

ここでは、`01_roll-dice` での内容を改良していきます。

- ダイスの並び替えが行えるようにする。
- `[[1, true], [3, false], [3, false], [5, false], [6, true]]` のように、ダイス値とロックを管理
- `html` 上にある5つのダイス要素は、上記の配列に追従して移動したりはしない
- 上記の内容をユーザーに表示するためのもの、というイメージ
- なのでダイスを振った際は一度状態をリセットし、ダイスの並びが確定した後で、リフレッシュするような
