# Finalize ToDo

- [ ]CSS stylelint（Web-Zukuri側）
- [ ]GH-Pages の webmanifest 確認（`start_url`）
- [ ]LogoIcon の生成
- [ ]`dice` と `priority` から配列を返す関数 `calcScore()` を作る？
- [ ]how-to_play.md（ja版含む）
- [ ]README.md
- [ ]index.html（ja版）
- [ ]スコアオブジェクト
  - `id`, `name`, `calcPoint` の３つ？
  - function `targetPoint()`, `totalPoint()`, `fixPoint()` 等を別途設け、`calcPoint` で指定？
  - 上記関数はダイス値を受け取り（fix は固定値も）、点数を計算させる？
- [ ]`document.documentElement.lang // -> "en"` とわかるので、`name` は `ja`, `en` を用意？
- [ ]用語統一（スコアとポイントとか、役名とか）
- [ ]ポイント変更（成立確率から）
- [ ]ユニット名欄のスタイル（4dvw? rem使用？）
- [ ]エネミーの最初らへんをテーブル使用に置き換える、assignmentは消去
- [ ]コンソールに出すのは選択されたものだけに？
- [ ]ScoreTableにUpperSectionもしくは別途場所に表示を
